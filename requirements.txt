yt-dlp>=2024.8.6
youtube-transcript-api>=0.6.2
openai>=1.40.0
anthropic>=0.34.0
python-dotenv>=1.0.1
pydantic>=2.8.0
pydantic-settings>=2.4.0
pyyaml>=6.0.2
apscheduler>=3.10.4
requests>=2.32.0
# Postgres driver for the Railway-managed DB (used when DATABASE_URL is set)
psycopg[binary]>=3.2.0
# Structured logging (JSON, context vars) — Phase 0
structlog>=24.4.0
